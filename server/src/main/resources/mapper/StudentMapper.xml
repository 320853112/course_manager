<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.denghuo.course_manage.dao.StudentDAO">
    <select id="getStuInfo" parameterType="Integer" resultType="com.denghuo.course_manage.model.Student">
        select id, name,college,major,class_name from student where id = #{id}
    </select>
</mapper>